{
  "name": "fantasy_football",
  "version": "1.0.0",
  "description": "A Fantasy Football Application",
  "main": "index.js",
  "engines": {
    "node": "7.10.0"
  },
  "scripts": {
    "client": "cd front-end && npm run start",
    "server": "cd server && npm run start",
    "start": "concurrently --kill-others \"npm run server\" \"npm run client\"",
    "pre": "cd front-end && npm install && cd ../server && npm install && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.0.1",
    "dotenv": "^6.1.0",
    "express": "^4.15.3",
    "node-fetch": "^2.2.0",
    "pg": "^6.2.2",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "eslint": "5.6.0",
    "eslint-config-google": "^0.11.0"
  }
}
